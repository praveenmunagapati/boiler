read_serial: reads the serial port device provided as the first argument 
read_serial.js: a version of the same written for node.js
symmetricom_serial: an example of a program that writes to a serial port
                    to set the time on a Symmetricom ND-4 clock 

See also libftdi, for USB-to-serial communication using the FTDI chipset.
